_FrameBoxSize = 0;

// Only read image from file when script is parsed
if (ExecMode & EM_CHANGE)
{
	imgTex = ReadImg("text.png");
}

// Display image
:imgTex;

// Set image as texure
SetTexture(imgTex);

// Draw plane spanned by
:Red;
:VecE3(1,1,0)^VecE3(0,1,1);
